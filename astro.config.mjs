// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
    site: "https://therealrainman.github.io",
    base: "/record-robotics-website-widgets",
});
